// Authoritative product release version for the PULSE platform.
// Note: package.json uses semantic development tags (e.g. 0.0.0 for local/workspace builds),
// while APP_VERSION defines the user-facing product release version (PULSE v2.1).
export const APP_VERSION = '2.1';
