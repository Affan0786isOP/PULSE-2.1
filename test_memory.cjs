const admin = require('firebase-admin');
const fs = require('fs');

console.log("Memory Map doesn't seem to have entries because the server was restarted and it only holds recent sessions, OR it requires submitting first.");
